# Core engine
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0
requests>=2.31
joblib>=1.3
tzdata>=2024.1

# HTTP API (api/) behind the web app
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0

# Tests and figures
pytest>=7.4
matplotlib>=3.8

# The daily data refresh (scripts/update_season.py) additionally needs
# soccerdata, which brings Selenium and a TLS client: pip install -e ".[data]"
