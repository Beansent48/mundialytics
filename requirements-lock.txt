# Known-good versions: the exact set the full local suite last passed with
# (Python 3.14, 2026-09-17). Not a hard lock — CI installs the ranges in
# requirements.txt on Python 3.10 and 3.12, where some of these pins don't exist.
fastapi==0.141.1
joblib==1.5.3
matplotlib==3.11.0
numpy==2.4.6
pandas==3.0.3
pydantic==2.13.5
pytest==9.1.1
pyyaml==6.0.3
requests==2.34.2
scikit-learn==1.9.0
scipy==1.17.1
soccerdata==1.9.0
tzdata==2026.2
uvicorn==0.49.0
