from __future__ import annotations
import pandas as pd
def normalize_sofascore_lineups(df: pd.DataFrame) -> pd.DataFrame: return df.copy()
def load_sofascore_lineups(*args, **kwargs) -> pd.DataFrame: return pd.DataFrame()
