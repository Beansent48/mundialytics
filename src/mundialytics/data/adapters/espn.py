from __future__ import annotations
import pandas as pd
def normalize_espn_lineups(df: pd.DataFrame) -> pd.DataFrame: return df.copy()
def load_espn_lineups(*args, **kwargs) -> pd.DataFrame: return pd.DataFrame()
